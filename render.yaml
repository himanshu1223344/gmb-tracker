cd C:\Users\LENOVO\gmb-tracker

# Create the file with this content:
cat > render.yaml << 'EOF'
services:
  - type: web
    name: gmb-tracker
    runtime: python
    runtimeVersion: '3.11.9'
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT server:app
EOF
